import 'package:flutter/material.dart';

class AppColors {
  // Warna Aksen Utama (Hijau Stabilo/Neon dari tombol/ikon)
  static const Color primary = Color(0xFFD4FF00); 
  
  // Warna Gelap (Untuk Bottom Nav, Tombol Gelap, Background Splash)
  static const Color dark = Color(0xFF121212);
  
  // Warna Terang (Untuk Background Card/BottomSheet)
  static const Color light = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF5F6F8); // Abu-abu super muda
  
  // Teks
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFF757575);
}